{
  "scripts": {
    "start:parcel": "parcel index.html",
    "start:db": "json-server -w db.json",
    "build": "tsc -w",
    "test": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "jest": "^24.9.0",
    "ts-jest": "^24.2.0",
    "typescript": "^3.7.3"
  },
  "dependencies": {
    "axios": "^0.19.0"
  }
}
